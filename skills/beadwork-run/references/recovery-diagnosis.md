# 技术失败诊断

根因不明的技术失败先诊断，再决定是否恢复。诊断保留失败证据、关键假设和最小修复建议；只读 scout 的局部调查默认 `gpt-6-luna` / `high`，根因未明或跨模块矛盾使用 `gpt-6-sol` / `medium`，困难诊断使用 `gpt-6-sol` / `high`；动态复现使用一次性环境。诊断结果按 `recovery-blocked.md` 的 outcome 与阶段规则处理，不增加停止计数。同一问题再次出现时重新检查假设，必要时使用已有模型升级入口。诊断与恢复均沿用正常验收和验证契约。
